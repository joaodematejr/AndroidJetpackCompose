package com.demate.appfundamentals

fun main() {
    /**
     * Operators
     * +, -, *, /, %
     * ++, --
     * +=, -=, *=, /=, %=
     * ==, !=, >, <, >=, <=
     * &&, ||, !
     * ?:, !!
     * .., in, !in
     * is, !is
     * as, as?
     * .., until, step
     * ..<, downTo
     * shl, shr, ushr, and, or, xor, inv
     * .., downTo, step
     */

    val a: Int = 10
    val b: Int = 20
    val c: Double = 30.0
    val d: Double = 40.0
    println(a + b)
    println(c + d)

}